export interface ScaffoldOptions {
  projectName: string;
  outDir?: string;
  description?: string;
  author?: string;
  force?: boolean;
  noInstall?: boolean;
  noGit?: boolean;
  template?: string;
}
